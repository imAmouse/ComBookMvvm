<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.base" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppTheme" parent="AppTheme.base">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/centerColor</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="colorAccent">@color/centerColor</item>
        <item name="android:windowBackground">@color/app_bg_color</item>
    </style>

    <style name="AppTheme.Black" parent="AppTheme">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <style name="AppTheme.Background.Transparent" parent="AppTheme.base">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:activityOpenEnterAnimation">@anim/bookinfo_in</item>
        <item name="android:activityOpenExitAnimation">@anim/bookinfo_out</item>
    </style>

    <style name="AppTheme.Background.Black" parent="AppTheme.Background.Transparent">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <style name="AppTheme.Fullscreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.Fullscreen.AppStart" parent="AppTheme.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

</resources>
